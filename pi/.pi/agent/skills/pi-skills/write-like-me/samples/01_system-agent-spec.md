i want to build a pi-agent for system housekeeping. i have a few special system services at this point (forget the names of these exactly; you should figure this out for me). i want system-agent to run every hour and check the status of each of these services. pi-agent should debug failing services and try and fix them. it should also turn on stopped services if ENABLE_STOPPED_SERVICES=1. pi-agent should also write a notification to ntfy.sh (topic named system-agent-notifications-a81457). it should do this both when it successfully fixes a service, and when it fails to fix a service. message format should be standard: (status: resolved, service_name: <NAME>, problem: <PROBLEM>, actions_taken: <ACTIONS>). we should schedule system-agent on a cron job so it runs every hour. system-agent should run with '--provider llama-cpp --model Qwen3-Coder-30B-A3B-Instruct-UD-IQ3_XXS.gguf'. this agent should run headless, and should never ask for user input. we have a couple ways of doing that (calling 'pi' directly is one, using the typescript bindings is another). do some research and suggest pros and cons to each approach before we implement anything
