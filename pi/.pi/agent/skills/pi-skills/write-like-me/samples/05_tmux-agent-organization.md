in general im wondering if there is a good way to organize agents in tmux. i have been adding the agent's session id to each tmux window i spin up, which has been making organization nicer. standards would be nice. one concrete thing i think we can implement very easily is subagents (via a skill). specifically, we could tell agents that they can spin up subagents in a dedicated tmux session (named tmux). we would require that the agent spin up all subagents in a session named after it (e.g. agent 01a052e1's subagents should show up in tmux session 'subagents', window '01a052e1). the agent could then control these agents with the tmux skill and monitor as needed. we'd need to nail down specific requirements for when subagents are useful. easy solution for the moment is to just say "don't spin up a subagent unless specifically directed by a human user" and leave it at that? subagents are weird, i think they literally just exist to save context and/or prevent the model from going "off the rails" solving a tangential problem.
