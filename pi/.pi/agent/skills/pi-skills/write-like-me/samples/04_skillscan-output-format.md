great. let's talk about the output format. i want to present results in a JSON formatted report. iirc pi-agent does NOT support schemas, so we will probably need a two step process where we generate a detailed plain text report, then use simon willison's 'llm' with --schema-multi to output report results. i think we should add a section to the report for every skill scanned (including the FULL path to the skill), along with a top level description and threat level (low, medium, high, extreme). for each skill, we should then have a detailed section where the agent says what vulnerabilities it scanned for, any suspicious sections, follow ups, etc. then when we do the json schema approach we can get results in the form of [{"skill_path": "/full/path/to/skill.md", "threat_level": "low | medium | high | extreme", "details": "text explaining details of the report"}, ...]. do note that we'll generate this structured output in a further processing step; pi-agent should not have to worry about this directly
