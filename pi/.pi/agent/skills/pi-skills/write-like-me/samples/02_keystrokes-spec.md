i am SSHd into this box from another machine. i want to wire in speech capabilities to my agent. i have a script on the local (non-SSHd into) box that transcribes from voice to text. i'd like to turn the text into key presses in a tmux pane. what is the best, easiest approach for doing this? should i have some kind of send script on the local box side, have that transcribe the text + call some program to send it to the SSH'd box, then have another program on the SSH'd box that listens for characters coming in from a port and sends them to the specified pane? this could even be one script where we have a --send flag for the sender, and a --listen side for the listener. the listener side should have to specify a pane (somehow) indicating what pane they want to send the raw 'keystrokes' that come in to. the script should also include an --enter option (enabled by default) which hits 'enter' at the very end to make sure the agent watching in the tmux pane sees the prompt. write this up for me. actually: instead of go let's use typescript. write this in ~/repos/keystrokes. use the keystrokes.py script
